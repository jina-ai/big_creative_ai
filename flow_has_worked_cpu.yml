jtype: Flow
with:
  env:
    JINA_LOG_LEVEL: DEBUG
jcloud:
  version: '3.12.0'
  labels:
    project: big-creative-ai
executors:
  - name: dreambooth
    uses: jinahub+docker://2h1j2v7o/v0.0.1-test-2
    uses_with:
      hf_token: ${{ ENV.HF_TOKEN }}
    env:
      JINA_LOG_LEVEL: DEBUG